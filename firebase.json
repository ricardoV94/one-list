{
  "firestore": {
    "rules": "firestore.rules"
  },
  "hosting": {
    "predeploy": ["bash stamp-deploy.sh"],
    "public": ".",
    "ignore": ["**/.*", "**/.*/**", "**/node_modules/**", "firebase.json", ".firebase", "node_modules", "README.md", "*.md", "*.sh", "firestore.rules", ".git", ".gitignore", "mocks/**", "tests/**"],
    "headers": [
      {
        "source": "sw.js",
        "headers": [{ "key": "Cache-Control", "value": "no-cache" }]
      }
    ]
  }
}
